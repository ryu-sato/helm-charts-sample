apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-charts-sample-v3.fullname" . }}
  labels:
    {{- include "helm-charts-sample-v3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-charts-sample-v3.selectorLabels" . | nindent 4 }}
